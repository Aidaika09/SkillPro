<h1>Project Lists</h1>

<h2>All Projects:</h2>
<ul>
  {{#each projects}}
    <li>
      <strong>{{this.name}}</strong> - {{this.description}} ({{this.status}})
    </li>
  {{/each}}
</ul>

<h2>Completed Projects:</h2>
<ul>
  {{#each completedProjects}}
    <li>
      <strong>{{this.name}}</strong> - {{this.description}} ({{this.status}})
    </li>
  {{/each}}
</ul>
